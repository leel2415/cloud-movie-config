spring:
  datasource:
    initialize: true
    platform: h2
    url: jdbc:h2:mem:moviedb;
    username: sa
    password: null
  h2:
    console:
      enabled: true
 
logging:
    path: ../log/${spring.application.name}

name: jungsuk-default hihi
